const Embetty = require('./index.js')

async function run() {
  const embetty = new Embetty()

  // Tweet
//  const tweet = await embetty.loadTweet('1029854456106639360')
//  console.log(tweet)
/*
  // Instagram
  const instagram = await embetty.loadInstagramPost('BmqKI6rFfSD')

  // YouTube
  const yt = await embetty.loadYoutubeVideo('aybsqxoYPUM')

  // image
  // profilbild
  // tweet

  post.getImage().then((url) => {
    console.log(url);
  })
*/
}

run();
